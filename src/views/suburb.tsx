import React from "react";
import NavBar from "../components/navbar";

const SuburbView = () => {
    return (
        <div>
            <NavBar></NavBar>
        </div>
    );
};

export default SuburbView;
